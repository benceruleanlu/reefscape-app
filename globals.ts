import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  rootView: {
    flex: 1,
    padding: 40,
    paddingTop: 25,
    paddingBottom: 25,
    backgroundColor: "white"
  }
});
